name: notekey_app
description: NOTEkey – Musiklern-App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.4.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.1
  firebase_storage: ^12.3.0

  # Medien & Pfade
  image_picker: ^1.1.2
  path_provider: ^2.1.4
  path: ^1.9.0

  # Sonstiges
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.0
  sqflite: ^2.3.3
  webview_flutter: ^4.4.2
  uuid: ^4.2.2
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/notekey1024.png"
